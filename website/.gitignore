/node_modules
/.next
/out
/build
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# User-uploaded website images (managed via the admin app). Keep the folder,
# ignore its contents.
/public/uploads/*
!/public/uploads/.gitkeep
